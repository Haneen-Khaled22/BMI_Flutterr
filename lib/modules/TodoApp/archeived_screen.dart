import 'package:flutter/material.dart';

class ArcheivedScreen extends StatelessWidget {
  const ArcheivedScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
